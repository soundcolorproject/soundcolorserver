
:root {
  --icon-color: var(--white);
}

.icon {
  display: inline-block;
  fill: var(--icon-color);
}

:global(#svg-container) {
  position: fixed;
  left: -10000px;
  top: -10000px;
  display: none;
  opacity: 0;
  width: 0;
  height: 0;
  pointer-events: none;
}
