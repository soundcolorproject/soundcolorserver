
.pattern-selector {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;

  .pattern-option {
    background: var(--white);
    color: var(--grey-90);
    margin: 0;
    padding: 22px 0;
    flex-basis: 50%;

    &:hover {
      background: var(--grey-10);
    }

    &:active {
      background: var(--grey-20);
    }

    &.selected {
      background: var(--grey-60);
      color: var(--white);
    }
  }
}
