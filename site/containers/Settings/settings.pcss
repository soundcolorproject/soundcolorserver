
.settings {
  padding: 12px 0;
  display: flex;
  flex-direction: column;

  .over-under {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;

    .over {
      font-weight: 600;
    }

    .under {
      font-weight: 700;
    }
  }
}
